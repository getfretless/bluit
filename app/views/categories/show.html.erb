<% sidebar @category.sidebar %>

<div class="category">
  <h2><%= @category.name %></h2>
  <div class="description"><%= @category.description %></div>
</div>

<%= render 'posts/list', posts: @posts %>
